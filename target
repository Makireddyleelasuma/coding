class Solution {
    public int searchInsert(int[] nums, int target) {
        if(n < 1) {
            return 0;
        }
        
        int low = 0;
        int high = n-1;
        int mid;
        int pos;
        while(low <= high) {
            mid = low + (high-low)/2;
            
            if(A[mid] == target) {
                return mid;
            } else if(A[mid] > target) { 
            
